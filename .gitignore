# Dependencies
vendor/
node_modules/
composer.lock

# Runtime / Logs
src/logs/
*.log

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo

# Local secrets (if any)
.env
.env.local

# Docker volumes (data)
data/
*.sqlite

# Build artifacts
dist/
build/
*.tar.gz

# DB snapshot (keep .sql.gz but not raw .sql — too large)
db-init/*.sql
!db-init/*.sql.gz
db-init/*.bak

# Editor backups
*~
*.bak

# OS specific
$RECYCLE.BIN/
desktop.ini
